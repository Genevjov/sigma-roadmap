version: '3.1'

services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: player_service
      POSTGRES_USER: playerService
      POSTGRES_PASSWORD: boll#plank

  pgadmin:
    image: chorss/docker-pgadmin4
    ports:
      - 5050:5050
